# Production API base for the admin app (public HTTP API — not a secret).
# Baked into the Vite build; the deployed AlphaBeacon backend on AWS (us-east-1).
VITE_API_URL=https://m711qq2a30.execute-api.us-east-1.amazonaws.com
